//import { undoable } from "./undoable";
import { newundoable } from "./newundoable";
import { shapes } from "./shapes";

//export const history = undoable(shapes);
export const history = newundoable(shapes);
